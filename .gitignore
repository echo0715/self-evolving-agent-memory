__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
runs/
*.jsonl
!examples/**/*.jsonl
.venv/
# `appworld download data` defaults --root to the CWD, so a stray invocation
# drops ~160 MB of benchmark payload here. Benchmark data belongs on scratch.
data/
results/
